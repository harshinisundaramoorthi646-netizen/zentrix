[build]
  publish = "dist"
  command = "npm run build"

# Proxy API calls to live API server if configured
[[redirects]]
  from = "/api/*"
  to = "/api/:splat"
  status = 200

# Single Page Application (SPA) routing fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
